#news_page.grid_16
  .blog_title 
    .blog_name Knotes From the Underground
    .blog_tag Site news, physics jokes and winning Lotto numbers from Joe and Tom
  .grid_13.alpha
    #blog= render Post.all(:order => "updated_at DESC")
  .grid_3.omega= render :partial => 'pages/skyscraper_ad'
  .clear
  - if current_user && current_user.is_admin?
    #news_post
      %h3 Create a New Post
      %hr
      - form_for Post.new do |f|
        = f.label :title
        %br
        = f.text_field :title
        %br
        = f.label :content
        %br
        = f.text_area :content, :class => "RTE", :id => 'tinymce'
        %br
        = f.submit "Publish"
.clear

- content_for :javascript do
  = render :partial => 'shared/tinymce'
  :javascript
    $(function(){
      knotebooks.load_tinymce('post_content');
    });
